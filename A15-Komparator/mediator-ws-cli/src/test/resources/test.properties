#uddi.enabled=true

#uddi.url=${uddi.url}
#ws.name=${ws.name}

# ws url below is used only if uddi is disabled above
#ws.url=${ws.url}

uddi.enabled=${uddi.enabled}
uddi.url=${uddi.url}

# mediator
ws.name=${ws.name}
# ws url below is used only if uddi is disabled above
ws.url=${ws.url}

# supplier
supplier.ws.name=${supplier.ws.name}
# ws url below is used only if uddi is disabled above
supplier.ws.url=${supplier.ws.url}